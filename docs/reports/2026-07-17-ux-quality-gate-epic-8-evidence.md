# EPIC-8 Pilot Evidence Matrix

Frozen build for every row: `ce5a389a01a8718f220d5766181aec0730c4832e`.

All rows use local Auth/Firestore emulators and synthetic/de-identified data. Screenshots were inspected in the in-app browser; this text matrix is the persisted non-sensitive evidence artifact.

## Capability and lifecycle record

Harness: in-app Browser plugin `26.715.31251`, one Browser session, frozen Vite build at `127.0.0.1:4178`, Auth emulator at `127.0.0.1:9099`, and Firestore emulator at `127.0.0.1:8080`. The uninterrupted run used synthetic identity `peach.otter.880@example.com`, emulator UID `1eG2RufoYt92sWiRXie5qoitATwg`, and expected storage path `users/1eG2RufoYt92sWiRXie5qoitATwg/history`. Source confirms `getHistory` and `saveWorkout` use `users/{uid}/history`; focused storage tests assert the `getHistory` path and `saveWorkout` behavior. The rules protect all nested `users/{uid}` documents.

The per-run capability probe returned only `visibility` and `viewport`. There was no actual browser-zoom, notched-safe-area, reduced-motion, or sequential-keyboard capability. A bounded keyboard probe found no locator focus operation and a Tab press could not complete reliably. Failure alone was not treated as proof: the documented capability list established the unsupported portions. Each selected alternative is the approved, safest local approximation that avoids production data, new installs, or an unfaithful emulator restart; the approved fallbacks supplied the evidence obligations below.

| Area | Applicability | Capability state and reason | Probe / eligible alternatives / selected fallback | Evidence kind and outcome | Obligation / disposition / residual risk / reactivation |
| --- | --- | --- | --- | --- | --- |
| Sequential keyboard | Applicable to interactive generator, workout, and catalog controls. | `partial`; sequential traversal portion has `capability_reason: unsupported-by-harness`. | Capability list plus bounded locator focus/Tab probe. Eligible alternatives were semantic source audit and focused component tests; both were selected, with reliable rendered focus observations kept separate. | `source-audit` + `component-test`; semantics, native controls, disabled states, focus logic, and focused test coverage exist. `inconclusive` for real sequential traversal; existing error-focus defects remain defects. | `satisfied`; `nonblocking-residual`. Residual risk: real Tab order and assistive-technology traversal were not observed. Re-probe every future required run. |
| Actual browser zoom | Applicable to responsive content and fixed notice. | `partial`; actual page zoom has `capability_reason: unsupported-by-harness`, while viewport override is supported. | Capability list. Eligible alternatives were viewport reflow proxies; selected half of the 1280 CSS-pixel representative width. Requested and actual override: 640 CSS pixels, height 720. | `rendered-proxy`; no horizontal clipping or unreachable primary/utility action was observed at 640px, while E-21b preserves the separate 240px overflow defect. `not-tested` for actual zoom. | `satisfied`; `nonblocking-residual`. Residual risk: browser text/layout zoom behavior may differ from viewport reflow. Re-probe every future required run. |
| Notched safe area | Applicable because the emulator notice is fixed to the viewport edge. | `unsupported`; `capability_reason: unsupported-by-harness`. | Capability list. Eligible alternatives were rectangular reach evidence and source audit; both were selected. | `rendered-proxy` + `source-audit`; rectangular reach is covered in E-07/E-16, while no `viewport-fit` or `env(safe-area-*)` accommodation exists. `static-risk`. | `satisfied`; `nonblocking-residual`. Residual risk: controls or notice may conflict with a real inset. Re-probe every future required run. |
| Reduced motion | Applicable because frozen CSS contains multiple 0.2s/0.3s transitions. | `unsupported`; `capability_reason: unsupported-by-harness`. | Capability list. Eligible alternatives were rendered transition inventory and CSS/JavaScript preference audit; both were selected. | `rendered-proxy` + `source-audit`; transitions are present and no `prefers-reduced-motion` handling exists. `static-risk`. | `satisfied`; `nonblocking-residual`. Residual risk: preference behavior was not rendered. Re-probe every future required run. |

No cached waiver is created. These classifications apply only to this run, and future required reviews must probe current tooling again.

The compact scenario table below predates the amended column layout but inherits the same taxonomy for every row. Unless a row explicitly overrides a field: applicability is `applicable`; `capability_state` is `supported`; evidence kind is `rendered-primary`; the classification term is the outcome; and the evidence obligation is `satisfied` because the recorded action/state was observed. When a row records multiple outcomes, each is a separate subfinding with the same inherited metadata and its own disposition; E-06 therefore has an `observed-pass` retry subfinding and a `defect` announcement/focus subfinding. An `observed-pass` has disposition `not-applicable` because it creates no finding. A frozen-product `defect` or `static-risk` has disposition `nonblocking-residual` for this historical evaluation and still requires durable routing. `partial`, `inconclusive`, and `not-tested` rows use the explicit capability record above. Any direct TREK-206 changed-surface finding would instead be `blocking`; none was observed. These defaults do not erase any row-specific limitation.

| Scenario | Ref | Viewport | Normative expectation and action | Observed result | Classification / limitation |
| --- | --- | --- | --- | --- | --- |
| A Generate | E-01 | 375×812 | Signed-in ready state exposes Generate Plan and reachable constraints without horizontal overflow. | Header, two-column controls, slider, and primary action rendered. | `observed-pass`; keyboard focus and touch targets not measured. |
| A Generate | E-02 | 390×844 | Same mobile hierarchy remains operable at the second required width. | Same responsive structure; primary action visible in the card. | `observed-pass`; no quantitative target measurement. |
| A Generate | E-03 | 1280×800 | Wider layout keeps generator centered and utility navigation distinct. | Centered readable card and header utility; no console warnings/errors. | `observed-pass`; no production comparison. |
| A Generate | E-04 | Initial browser default (not recorded) | Signed-out and loading states must transition to a usable sign-in gate. | Loading state and signed-out Google button were observed before emulator sign-in. | `observed-pass` for the state transition only; the unrecorded viewport is insufficient for a responsive-layout claim. |
| A Generate | E-05 | 375×812 | Generate Plan produces a scrollable plan with locked/ready state explanation. | Five-exercise plan rendered with locked later sets and vertical scrolling. | `observed-pass`; fixed-notice reach and occlusion are covered in E-07. |
| A Generate | E-06 | 375×812 | Validation/application errors expose visible recovery. | With only the tracked local emulators stopped, Generate Plan showed “Failed to generate workout. Please try again.” Retrying after emulator restart removed the error and generated a result. The message had no `role`/`aria-live`, and focus was on `body` rather than the retry action. | `observed-pass` for visible retry/recovery; `defect` for announcement and focus recovery. |
| A Generate | E-07 | 375×812 | Long content must scroll without horizontal overflow and keep the fixed notice away from required controls. | Workout and catalog scrolled vertically with no horizontal overflow. At the exact 375 CSS-pixel viewport, Generate Plan ended at y=636 while the fixed notice began at y=763; at catalog maximum scroll the last actions ended at y=690. | `observed-pass` for full reach, no-overflow, and tested notice clearance. Safe-area insets remain separately classified in E-21c. |
| B Active workout | E-08 | 375×812 | Ready/work/locked states expose one clear next action. | Start Workout, ready Start set, work Confirm/Cancel, and locked explanations rendered. | `observed-pass`; collapse/expand is covered in E-14. |
| B Active workout | E-09 | 375×812 | A second Start is refused while one work timer runs, with current active-set guidance. | Bench Press start was refused; status named active Barbell Curl set 1. | `observed-pass` for guard; captured through DOM, not persisted video. |
| B Active workout | E-10a | 375×812 | Cancel retires the conflict status when the work timer stops. | Set became actionable, but concurrent-start status remained after 1.2s. | `defect`; confirmed frozen-commit issue linked to TREK-201. |
| B Active workout | E-10b | 375×812 | Confirm/Rest replaces the conflict status with current rest state. | Rest state rendered, but concurrent-start status remained after 1.2s. | `defect`; confirmed frozen-commit issue linked to TREK-201. |
| B Active workout | E-10c | 375×812 | Undo retires the conflict status when the completed set is reopened. | Set became actionable, but concurrent-start status remained after 1.2s. | `defect`; confirmed frozen-commit issue linked to TREK-201. |
| B Active workout | E-11 | 375×812 | Finish while a timer runs is blocked with recovery guidance. | Finish was blocked with “finish or cancel” guidance in the shared status region. | `observed-pass`; guidance is owned by the approved shared status region rather than repeated beside Finish. |
| B Active workout | E-12 | 375×812 | Zero-confirmation finish gives recoverable summary and disabled save. | 0/15 summary showed alert, disabled Save, and Back to workout. | `observed-pass`; incomplete path only. |
| B Active workout | E-13 | Browser default, uninterrupted lifecycle | A confirmed set saves to `users/{uid}/history`; the same identifying entry remains visible after reload without emulator/server/browser/auth restart. | Barbell Curl set 1 was confirmed and saved with duration `0:12`. Before reload, history showed July 17, 2026, Barbell Curl, set 1 Confirmed, work `0:08`. Reload retained authentication for UID `1eG2RufoYt92sWiRXie5qoitATwg`; after generating a plan again, the same entry and fields remained visible. | Capability `supported`; `rendered-primary`; `observed-pass`; obligation `satisfied`; disposition `not-applicable` because no finding remains. |
| B Active workout | E-14 | Browser default, same uninterrupted lifecycle | Collapse and interruption/reload preserve the identifiable active-set context or provide an approved explanation with actionable recovery/exit. | Collapse/expand passed in the earlier run. In the uninterrupted run, Overhead Press set 1 showed an active work timer. Reload retained authentication but silently returned to Generate Workout; no active context, explanation, recovery, or exit was shown. | Capability `supported`; `rendered-primary`; `defect`; obligation `satisfied`; disposition `nonblocking-residual` for TREK-206 because the frozen product is the evaluation target, not the task change surface. No usability pass is claimed; durable product routing is required. |
| C Catalog | E-15 | 1280×800 | Dense catalog remains readable, scrollable, and separates destructive actions. | Centered readable catalog with vertical scroll and Edit/Deactivate controls. | `observed-pass` visually; hierarchy not measured quantitatively. |
| C Catalog | E-16 | 375×812 | Catalog form/list reflows without horizontal overflow; Add must remain reachable and clear of the fixed notice. | Single-column form remained within the 375px viewport. Add was reachable above the fixed notice; at maximum list scroll the last catalog actions ended 73px above the notice. | `observed-pass` for reflow, reach, no-overflow, and tested notice clearance. |
| C Catalog | E-17 | 375×812 | Blank Add provides clear, proximal recovery guidance. | The name input was invalid (`required`, `valueMissing`, native validation message available), but its form has `noValidate`. Clicking Add left focus on Add and showed no inline, native, or live-region guidance. | `defect`; submission fails silently with no proximal recovery or focus handoff. |
| C Catalog | E-18 | 375×812 | Add, Edit/Save, Deactivate/Reactivate preserve catalog recovery. | Synthetic entry was added, renamed/saved, deactivated, and reactivated. | `observed-pass`; emulator-only mutation cleared at teardown. |
| C Catalog | E-19 | 375×812 | Route exit with unsaved edits has explicit preservation/discard behavior. | A synthetic catalog name was changed without Save, then Back to Generator was activated. No dialog or preservation/discard message appeared; reopening the catalog showed the original name and no unsaved value. | `defect`; unsaved edits are silently discarded. |
| D Cross-cutting | E-20 | All tested viewports | Unexpected console errors are absent in normal states but never substitute for usability evidence. | No warning/error entries were captured in the initial normal-state run. The deliberate emulator interruption produced expected Firestore connectivity/cache diagnostics. | `observed-pass` technical-only for normal states; interruption diagnostics are classified with E-06. |
| D Cross-cutting | E-21a | 375×812 | Keyboard focus order and focus recovery remain visible and coherent. | Programmatic/native focus observations showed an outline. The documented harness exposes no sequential-keyboard capability, and the bounded focus/Tab probe did not produce reliable traversal. Source audit found native controls, disabled states, a summary `tabIndex`, focus movement logic, and `:focus-visible` styles; focused component tests passed. E-06 and E-17 preserve actual error-focus defects. | `partial`; approved fallback obligation `satisfied`; sequential traversal remains `inconclusive`, not a pass. |
| D Cross-cutting | E-21b | 240px narrow proxy plus 640px half-width proxy | Content reflows without horizontal scrolling below required widths; actual browser zoom is separately classified. | The earlier requested 188px viewport was clamped to 240px and horizontally overflowed. This run requested and received a 640×720 override, half the 1280px representative width; generator controls, primary action, utility action, and fixed notice remained visible without observed horizontal clipping. | `defect` at rendered 240px; `observed-pass` for 640px rendered proxy only; actual zoom `not-tested`; fallback obligation `satisfied`. |
| D Cross-cutting | E-21c | Applicable mobile safe area | Fixed UI accounts for device safe-area insets. | Rectangular reach passed at tested positions. Capability probe exposed no safe-area emulation; source audit found no `viewport-fit` or `env(safe-area-*)` accommodation. | `static-risk`; approved fallback obligation `satisfied`; no safe-area pass is claimed. |
| D Cross-cutting | E-21d | Reduced-motion preference | Motion honors the user preference when applicable. | Rendered controls and catalog rows use 0.2s/0.3s transitions. Capability probe exposed no media-preference emulation; source audit found no `prefers-reduced-motion` handling. | `static-risk`; approved fallback obligation `satisfied`; no reduced-motion pass is claimed. |
| D Cross-cutting | E-21e | 375×812 | Interactive targets are at least 44×44 CSS pixels or have a documented contextual exception. | Generate Plan measured 42.7px high; muscle labels 41px; Add 35.3px; Close 32.7px; and Edit/Deactivate 29.3px. Several form controls measured 41.3–42.7px high. | `defect`; no contextual exception is documented. |

Reproduction metadata is the frozen build, named viewport, starting state, and action sequence recorded for each observed row. The active-timer branches are independently reproducible from E-09 → E-10a, E-10b, and E-10c. E-13 is Generate Plan → Start Workout → Start set → Confirm → Finish Workout → Save workout → Generate Plan → observe history → reload → Generate Plan → observe the same history. E-14 is Generate Plan → Start Workout → Start Overhead Press set 1 → observe work timer → reload → observe silent return to Generator. E-13 and E-14 shared one uninterrupted emulator/server/browser/auth lifecycle. E-06 used only the tracked local emulator stop/restart in the earlier interruption fixture; E-17 and E-19 used synthetic catalog state; E-21 measurements came from the rendered DOM at the stated CSS viewport. No screenshot files were persisted; live screenshots corroborated the textual observations.

## Current disposition

The approved capability-aware amendment now governs this pilot. Every applicable area has a per-run capability record and its prescribed evidence obligation is satisfied. Saved-history reload passed in one uninterrupted lifecycle. Active-workout resume produced a determinate frozen-product defect rather than an evidence gap. The four unsupported portions remain explicit residual risks and cannot be promoted to rendered passes.

TREK-206 recommendation: `evidence-complete-with-residual-capability-risk`. This is a recommendation about the completeness of the historical gate pilot, not a usability or product-acceptance pass. Direct TREK-206 changed-surface findings would block; none were observed. Frozen EPIC-8 product findings remain preserved and require durable duplicate routing. The stale timer status maps to TREK-201. Duplicate searches for `active workout`, `resume`, and `reload` found no existing task that owns reload recovery; CMT-41 on completed TREK-90 instead records it as previously out of scope. Creating or materially extending a product backlog item requires user approval before TREK-206 can receive its final Summary and completion disposition.
