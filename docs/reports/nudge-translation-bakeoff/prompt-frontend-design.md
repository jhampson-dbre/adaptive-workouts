# Session prompt — frontend-design

Work on TREK-229 in this worktree only.

Use `$frontend-design` for the design translation. Read and follow
`docs/reports/nudge-translation-bakeoff/run-brief.md` exactly, including its source
artifacts, locked content, allowed file scope, rendered evidence, verification, isolation,
and stopping rules.

This is a disposable implementation of the selected **A-r3 Warm Training Partner**
direction, not a production redesign and not a literal recreation of the concept image.
The statistical intelligence is the engine, not the interface aesthetic.

Before editing, report:

1. the current branch and commit;
2. that `frontend-design` is available;
3. that Impeccable is not available in this worktree;
4. the concise visual thesis you will execute.

Then complete and verify the full required output. Do not install or invoke Impeccable,
do not inspect the other bake-off branches or prompts, and do not commit, push, merge, or
update Trekker.

