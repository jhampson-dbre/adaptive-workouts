# TREK-245 authorized sign-out UX evidence

- Classification: `required` — session retirement, dirty-work recovery, responsive placement, focus, and protected-content lifecycle materially change.
- Approved scenario or artifact: `UX-ARTIFACT: trek-245-authorized-signout@v2` as recorded on TREK-245; the artifact file is absent from the repository, so the task acceptance criteria and approved combined dirty-work/canonical-cleanup contract are the executable scope.
- Build / commit: working tree on `codex/trek-245-approved-user-sign-out`, based on `34e2047`; Vite baseline emulator scenario `UX-10-04`, artifact `private-owner-approved-access@v2`.
- Viewport and starting state: requested `390x844`; in-app browser content viewport measured `375px` wide; synthetic approved emulator user with a recoverable workout.
- Actions: load the authorized recovery surface; inspect the shell control and focus; activate **Sign out**; cancel the native dirty-work confirmation.
- Observed result: **Sign out** was visible in the authorized header, exposed the expected accessible name, measured `63.2x44px`, and caused no horizontal overflow (`body` and header `scrollWidth === clientWidth === 375`). Initial focus remained on **Resume workout?**. Activating Sign out opened a native confirmation; canceling preserved the recovery surface and did not retire protected UI. Automated integration coverage verifies confirmation acceptance, immediate protected-UI retirement, focused rejection recovery, Login focus after success, Settings/Plan dirty aggregation, stale token suppression, and unchanged pending/error behavior.
- Rendered evidence: [`trek-245-authorized-signout-mobile.png`](../../.impeccable/evidence/trek-245-authorized-signout-mobile.png); focused suites `App.auth`, `App.lazyNavigation`, `Settings`, and `touchTargetContract` (60 passing).
- Material limitation: the browser-control session timed out while switching to the desktop capture and could not be cleanly finalized after two bounded attempts. The emulator scenario was stopped through its supported control command. Mobile is the product's primary operating context; CSS wrapping and touch-target contracts plus focused component tests provide the safe responsive alternative. Native confirmation chrome is browser-controlled and is therefore asserted behaviorally rather than captured as an application-owned surface.
