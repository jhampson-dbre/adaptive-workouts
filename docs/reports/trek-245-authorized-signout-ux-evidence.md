# TREK-245 authorized sign-out UX evidence

- Classification: `required` — session retirement, dirty-work recovery, responsive placement, focus, and protected-content lifecycle materially change.
- Approved scenario or artifact: `UX-ARTIFACT: trek-245-authorized-signout@v2` as recorded on TREK-245; the artifact file is absent from the repository, so the task acceptance criteria and approved combined dirty-work/canonical-cleanup contract are the executable scope.
- Build / commit: working tree on `codex/trek-245-approved-user-sign-out`, based on `34e2047`; Vite baseline emulator scenario `UX-10-04`, artifact `private-owner-approved-access@v2`.
- Viewport and starting state: synthetic approved emulator user with a recoverable workout at mobile (`375x844` measured content viewport) and desktop (requested and browser-reported `1440x900`; captured application raster `1425x891`).
- Actions: load the authorized recovery surface; inspect the shell control and focus; activate **Sign out**; cancel the native dirty-work confirmation on mobile; accept it on desktop.
- Observed result: **Sign out** was visible in the authorized header, exposed the expected accessible name, and measured `63.2x44px` at both viewports. Neither viewport had horizontal overflow. Initial focus remained on **Resume workout?**. Canceling preserved the recovery surface. Accepting on desktop removed the protected recovery region, rendered **Sign in with Google**, and focused that control. Automated integration coverage additionally verifies focused rejection recovery, Settings/Plan dirty aggregation, stale token suppression, and unchanged pending/error behavior.
- Rendered evidence: [`trek-245-authorized-signout-mobile.png`](../../.impeccable/evidence/trek-245-authorized-signout-mobile.png) and [`trek-245-authorized-signout-desktop.png`](../../.impeccable/evidence/trek-245-authorized-signout-desktop.png); focused suites `App.auth`, `App.lazyNavigation`, `Settings`, and `touchTargetContract` (60 passing).
- Harness reliability: the original timeout was isolated to sequential browser automation around the native confirmation: the click remains pending while the browser-owned dialog is open. Desktop verification starts the click and handles the confirmation concurrently, then waits for the signed-out surface. This completed in 3.5 seconds. No product sign-out timeout was observed.
- Material limitation: native confirmation chrome is browser-controlled and is asserted behaviorally rather than captured as an application-owned surface.
